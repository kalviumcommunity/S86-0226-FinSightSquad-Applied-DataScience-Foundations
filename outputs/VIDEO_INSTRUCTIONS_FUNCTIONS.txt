VIDEO WALKTHROUGH INSTRUCTIONS - Passing Data into Functions and Returning Results
=====================================================================================

Record a ~2 minute screen-capture demonstration.

Your video MUST include:

1. A function with parameters
   - Show the function definition clearly
   - Point out the parameter names
   
2. Passing arguments into the function
   - Call the function with different values
   - Show how arguments map to parameters
   
3. Returning a value
   - Show the return statement in the function
   - Explain that return sends data back
   
4. Using the returned result elsewhere
   - Store the returned value in a variable
   - Use it in a calculation or pass it to another function
   - Print the result OUTSIDE the function

DEMONSTRATION TIPS:

✓ Run: python scripts/functions_milestone.py
✓ Show your screen clearly with code visible
✓ Walk through at least 2-3 function examples
✓ Explain the difference between print and return
✓ Show how returned values enable reuse

COMMON MISTAKES TO HIGHLIGHT:

✗ Functions that only print (not return)
✗ Hardcoded values inside functions
✗ Not storing/using returned values

Video Requirements:
- Duration: ~2 minutes
- Screen-facing and clearly visible
- Audio clearly explaining what you're doing
- Show code execution and output

SUBMISSION:

1. Save your recording
2. Upload it where instructed by your course
3. Place the shareable link in `outputs/video_link.txt`

Example script for your video:
"Here I define a function calculate_discount that takes price and discount_percent as parameters.
When I call it with arguments 100 and 20, it returns 80.0. 
I store this in final_price and use it in further calculations.
This is better than printing inside the function because I can reuse the result."
