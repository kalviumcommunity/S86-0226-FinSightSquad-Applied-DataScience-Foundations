Video recording checklist (approx. 2 minutes)

1. Open and run `scripts/numeric_string_milestone.py`.
2. Show numeric examples (`int`, `float`) and arithmetic results.
3. Show string examples (concatenation, indexing, formatting).
4. Explain why mixing number + string directly causes a `TypeError`.
5. Demonstrate safe conversion using `str()` and `int()`.
6. Show type inspection output (`type()` / `isinstance()`).
7. Briefly summarize: "Type awareness prevents logical bugs."
8. Upload video and paste the link into `outputs/video_link.txt`.

Tips: Keep your screen clear, zoom text if needed, and speak slowly.
